{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/Outsmart-OOH/ooh_open_direct/master/schema/v1/resources/account/account_object.json",
    "type": "object",
    "title": "Account object",
    "description": "An account defines a buyer-advertiser relationship.",
    "default": {},
    "additionalProperties": false,
    "properties": {
        "$schema": {
            "type": "string",
            "format": "uri",
            "description": "$schema only included to aid validation of JSON, should not be used in real cal.ls."
        },
        "AdvertiserId": {
            "$id": "#/properties/AdvertiserId",
            "type": "string",
            "title": "Advertiser ID",
            "description": "An ID that identifies the organization that is acting as the advertiser. Advertiser ID may be generated by the buyer (agency) or by the publisher if the advertiser is also the buyer. An advertiser that is representing itself must have an AdvertiserId and BuyerId that match.",
            "minLength": 1,
            "maxLength": 36
        },
        "BuyerId": {
            "$id": "#/properties/BuyerId",
            "type": "string",
            "title": "Buyer ID",
            "description": "An ID that identifies the organization that is acting as the buyer. The Publisher generates the BuyerId. If the advertiser is performing their own buys, AdvertiserId and BuyerId must be the same.",
            "minLength": 1,
            "maxLength": 36
        },
        "ThirdPartyId": {
            "$id": "#/properties/ThirdPartyId",
            "type": "string",
            "title": "Thirdparty ID",
            "description": "An ID that identifies the organization upon who's behalf an Order is being placed (e.g. a group media agency).",
            "minLength": 1,
            "maxLength": 36
        },
        "Id": {
            "$ref": "https://raw.githubusercontent.com/Outsmart-OOH/ooh_open_direct/master/schema/v1/resources/account/account_id_string.json"
        },
        "Name": {
            "$id": "#/properties/Name",
            "type": "string",
            "title": "Account Name",
            "description": "The name of the account. Used for display purposes"
        },
        "ProviderData": {
            "$id": "#/properties/ProviderData",
            "type": "string",
            "maxLength": 1000,
            "title": "Provider Data",
            "description": "An opaque CLOB of provider-defined data. Providers may use this field as needed (for example, to store an ID that correlates this object with resources within their system). Note that any provider that edits this object may override the data in this field. The data should include a marker that you can identify to ensure the data is yours."
        },
        "Status": {
            "$id": "#/properties/Status",
            "type": "string",
            "title": "Status",
            "description": "A value that indicates the current state of the Account.",
            "enum": [
                "Pending",
                "Approved",
                "Disapproved"
            ]
        },
    }
}
